# ── Python version ────────────────────────────────────────────────────────────
# Requires Python 3.11.x  (see runtime.txt)

# ── Critical version pin: httpx must stay < 0.28 ─────────────────────────────
# httpx 0.28+ removed the `proxies` kwarg that older openai SDK expects.
httpx==0.27.2

# ── FastAPI stack ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.0
sse-starlette==2.1.0
pydantic==2.7.0

# ── Production server ─────────────────────────────────────────────────────────
# gunicorn is included as an alternative process manager if Railway needs it.
gunicorn==22.0.0

# ── Numpy (must stay < 2 for binary compat with sklearn/scipy) ───────────────
numpy==1.26.4

# ── LangChain 0.2.x family (all versions must match) ─────────────────────────
langchain==0.2.0
langchain-community==0.2.0
langchain-openai==0.1.7
langchain-core==0.2.0
langchain-text-splitters==0.2.0
langchain-pinecone==0.1.1

# ── Pinecone (compatible with langchain-pinecone 0.1.1) ──────────────────────
pinecone-client>=3.2.2,<4.0.0

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai==1.30.0
tiktoken>=0.7.0

# ── ML / Embeddings ───────────────────────────────────────────────────────────
transformers==4.36.2
sentence-transformers==2.7.0

# ── Search + BM25 ────────────────────────────────────────────────────────────
tavily-python==0.3.3
rank-bm25==0.2.2

# Note: pymupdf removed — not needed at runtime (PDFs excluded from git).
# Install locally if you need to re-index Pinecone or rebuild BM25 pkl cache:
#   pip install pymupdf==1.24.0
